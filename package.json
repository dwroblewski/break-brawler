{
  "name": "break-brawler",
  "version": "0.1.0",
  "description": "Drum'n'Bass arcade game - spec-first implementation",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "cucumber-js tests/**/*.feature",
    "test:watch": "cucumber-js tests/**/*.feature --watch"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^10.0.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "tone": "^14.7.0"
  }
}